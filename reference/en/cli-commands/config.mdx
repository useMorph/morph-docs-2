---
title: 'config'
---

The `morph config` command is used to set up your Morph account in the cloud to your local environment.

The result will be saved in `~/.morph/credentials`.

```bash Shell
morph config
```
