---
title: 'Plotlyを使ってSQLで取得したデータを可視化する'
icon: "chart-area"
---

Morphを使うとPythonのライブラリを使用してデータを簡単に可視化できます。
このチュートリアルではPlotlyを使用して、リッチなダッシュボードを簡単にチームメンバーに共有します。

## 事前準備

- このチュートリアルでは、以下のPythonライブラリを使用します。

```bash
morph-data
plotly
```

## 最終的な成果物

<Tabs>
    <Tab title="SQL">
        ```sql
        ```
    </Tab>
    <Tab title="Python">
        ```python
        ```
    </Tab>
    <Tab title="MDX(pages)">
        ```typescript

        ```
    </Tab>
</Tabs>